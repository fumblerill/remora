# Remora

Remora — это самодостаточный аналитический конструктор: Rust‑бэкенд принимает наборы данных и управляет пользователями, а фронтенд на Next.js предоставляет визуальный интерфейс для создания интерактивных таблиц, графиков и текстовых отчётов. Проект готов к публикации в открытом доступе и поддерживает как локальную разработку, так и запуск в Docker Compose.

## Возможности

- **Конструктор дашбордов.** Визуальный интерфейс позволяет добавлять виджеты (таблицы, графики, отчёты), менять их параметры и сохранять конфигурации.
- **Импорт данных.** Модальное окно загрузки принимает CSV/XLSX, помогает очистить данные и включает превью с валидацией.
- **Продвинутые таблицы.** Пивот-конфигурации, фильтры, сортировка, виртуализация строк, копирование значений и блок агрегации «как в графиках».
- **Графики.** Столбчатые, линейные и круговые диаграммы с легендой, фильтрами и агрегацией по оси X.
- **Текстовые отчёты.** Шаблоны с плейсхолдерами, условными метриками и форматированием чисел/дат.
- **Аутентификация и роли.** Бэкенд использует JWT, Argon2 и SQLite, пользовательскими правами управляет админка.

## Стек

- **Бэкенд:** Rust 1.78+/nightly, Axum, SQLx (SQLite), JWT, Tokio.
- **Фронтенд:** Next.js 15, React 19, TailwindCSS, TanStack Table/Virtual, Recharts, Biome для lint/format.
- **Инфраструктура:** Docker/Docker Compose, dotenv‑конфигурация, общая `.env`.

## Требования

- Docker и Docker Compose (для быстрого запуска) **или**
  - Node.js ≥ 20 (в Docker используется Node 22)
  - npm ≥ 10
  - Rust toolchain (stable; Dockerfile собирает на nightly)
- SQLite (поставляется автоматически; данные лежат в `backend/data`)

## Быстрый старт (Docker Compose)

```bash
git clone https://github.com/<your-org>/remora.git
cd remora
cp .env.example .env
# ← заполните JWT_SECRET и при необходимости BASE_URL/NEXT_PUBLIC_API_BASE
docker compose up --build
```

Сервисы:

- `http://localhost:3000` — веб-интерфейс (Next.js)
- `http://localhost:8080` — API Axum (если открыт в compose)

Docker монтирует `backend/data` и `web/public/configs`, поэтому локальные изменения/экспорт не пропадут.

## Локальная разработка без Docker

1. **Настройте окружение**
   ```bash
   cp .env.example .env
   # задайте JWT_SECRET, BASE_URL, INTERNAL_API_URL и т.п.
   ```

2. **Запустите бэкенд**
   ```bash
   cd backend
   cargo run
   ```

3. **Запустите фронтенд**
   ```bash
   cd web
   npm install
   npm run dev
   ```

4. Фронтенд автоматически проксирует `/api/*` к Next.js API-роутам, которые дальше обращаются к Rust‑сервису через `INTERNAL_API_URL`.

### Полезные команды

| Область    | Команда                        | Назначение                         |
|------------|--------------------------------|------------------------------------|
| Backend    | `cargo test`                   | Тесты бэкенда                      |
|            | `cargo fmt && cargo clippy`    | Форматирование и lint              |
| Frontend   | `npm run lint`                 | Biome check (нужен `.env`)         |
|            | `npm run format`               | Biome format                       |
| Docker     | `docker compose up --build`    | Полный запуск                      |

> ℹ️ Biome ожидает .env в корне `web` (скрипты уже делают `source ../.env`). Убедитесь, что `.env` существует перед запуском lint/build.

## Переменные окружения

Основные ключи берутся из `.env` (см. `.env.example`):

- `PROJECT_NAME` — отображаемое имя проекта.
- `BASE_URL` — публичный URL фронтенда (для CORS/ссылок).
- `INTERNAL_API_URL` — адрес бэкенда, к которому обращается Next.js (например, `http://localhost:8080` или `http://remora_backend:8080` в Docker).
- `NEXT_PUBLIC_API_BASE` — явный URL API, если фронт и бэк на разных доменах (по умолчанию proxy `/api/*`).
- `CORS_ORIGINS` / `CORS_ORIGINS_TEMPLATE` — допускаемые источники.
- `JWT_SECRET` — обязательный секрет для подписи токенов.

Дополнительные переменные (`API_URL`, `FRONTEND_ORIGIN` и т.д.) можно раскомментировать в `.env.example`, если нужно жёстко задать внешние адреса.

## Структура репозитория

```
backend/        # Axum + SQLx сервер (авторизация, загрузка файлов, экспорт)
  src/
  data/         # SQLite база (монтируется в Docker)
  Dockerfile
web/            # Next.js 15 приложение с настройщиком дашбордов
  components/
  public/configs/ # JSON-конфиги дашбордов (монтируются в Docker)
  Dockerfile
docker-compose.yml
.env.example
```

## Вклад и обратная связь

1. Форкните репозиторий, создайте ветку с понятным названием (`feature/aggregation`, `fix/chart-loop` и т.п.).
2. Покройте изменения тестами/линтом (см. команды выше).
3. Создайте Pull Request с описанием проблемы и решением.

Issues приветствуются: указывайте шаги воспроизведения, логи и информацию об окружении.

---
🤖 Примечание: этот проект был разработан при использовании искусственного интеллекта.

