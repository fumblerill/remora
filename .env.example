# ──────────────────────────────
# 🌍 Общие
PROJECT_NAME=remora

# Для продакшна можно указать общий базовый URL (протокол + домен), например https://app.example.com
BASE_URL=http://localhost
# URL, по которому Next.js внутри кластера/compose достучится до Rust-бэка
INTERNAL_API_URL=http://remora_backend:8080
# Если нужно явно указать базовый путь для браузерных запросов (например, при вынесении API на иной домен),
# задайте NEXT_PUBLIC_API_URL. По умолчанию фронт обращается к /api/*, и Next проксирует запросы внутрь.
NEXT_PUBLIC_API_BASE=
# При необходимости можно переопределить явные адреса, раскомментировав строки ниже
# API_URL=
# NEXT_PUBLIC_API_URL=
# FRONTEND_ORIGIN=
# NEXT_PUBLIC_FRONTEND_ORIGIN=

# 🌐 CORS
# Если нужно задать конкретные адреса (например, https://example.com),
# пропишите их в CORS_ORIGINS через запятую. Иначе используется шаблон ниже.
CORS_ORIGINS={BASE_URL}
CORS_ORIGINS_TEMPLATE=http://localhost:3000,http://127.0.0.1:3000,http://0.0.0.0:3000

# ──────────────────────────────
# 🔐 JWT
JWT_SECRET=
